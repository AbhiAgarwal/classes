# Microarray data for two bacteria. Other data related to this pair of matrices such as: 
# a list of pairs of genes that are related (orthologs) between these species and the 
# functional classes for these genes will also be provided.

load('baa.ratios.rda')
str(ratios)
length(ratios)
summary(ratios)